<MainWindow>:
    orientation: 'vertical'
    log_label: log_label
    table: table
    title: title
    ActionBar:
        pos_hint: {'top':1}

        ActionView:
            use_separator: True
            ActionPrevious:
                id: title
                title: 'Action Bar'
                with_previous: False
            ActionButton:
                text: 'Both'
            ActionButton:
                text: 'Switch'
                on_press: root.switch_week()
            ActionGroup:
                text: 'Types'
                ActionToggleButton:
                    id: group_button
                    group: 'type'
                    text: 'Group'
                    icon: './static/group.png'
                    on_press: root.show_group_popup()
                ActionToggleButton:
                    id: teacher_button
                    group: 'type'
                    text: 'Teacher'
                    icon: './static/teacher.png'
                    on_press: root.show_teacher_popup()
                ActionToggleButton:
                    id: room_button
                    group: 'type'
                    text: 'Room'
                    icon: './static/room.png'
                    on_press: root.show_room_popup()
            ActionGroup:
                mode: 'spinner'
                text: 'Menu'
                ActionButton:
                    id: login_button
                    text: 'Login'
                    size_hint_y: None
                    height: 40
                    on_press: root.show_login_popup()
                ActionButton:
                    id: logout_button
                    text: 'Logout'
                    size_hint_y: None
                    height: 40
                    on_press: root.logout()
                ActionButton:
                    text: 'Exit'
                    size_hint_y: None
                    height: 40
                    on_press: app.stop()
    BoxLayout:
        id: table
    Label:
        id: log_label
        size_hint_y: 0.05
        pos_hint: {'bottom': 1}
        text: 'Success'
        font_size: root.log_label.height * 0.8
        padding_x: 10
        canvas.before:
            Color:
                rgba: .5, .5, .5, 1.0
            Rectangle:
                pos: self.pos
                size: self.size