<MainWindow>:
    orientation: 'vertical'
    log_label: log_label
    table: table
    LessonBar:
        pos_hint: {'top':1}

        ActionView:
            use_separator: True
            ActionPrevious:
                title: 'Action Bar'
                with_previous: False
            ActionOverflow:
            ActionButton:
                text: 'Prev'
            ActionButton:
                text: 'Next'
            ActionGroup:
                text: 'Types'
                ActionToggleButton:
                    id: group_button
                    group: 'type'
                    text: 'Group'
                    icon: './static/group.png'
                    on_press: root.show_group_popup()
                ActionToggleButton:
                    id: teacher_button
                    group: 'type'
                    text: 'Teacher'
                    icon: './static/teacher.png'
                    on_press: root.show_teacher_popup()
                ActionToggleButton:
                    id: room_button
                    group: 'type'
                    text: 'Room'
                    icon: './static/room.png'
                    on_press: root.show_room_popup()
            ActionGroup:
                mode: 'spinner'
                text: 'Menu'
                ActionButton:
                    id: login_button
                    text: 'Login'
                    size_hint_y: None
                    height: 40
                    on_press: root.show_login_popup()
                ActionButton:
                    id: logout_button
                    text: 'Logout'
                    size_hint_y: None
                    height: 40
                    on_press: root.logout()
                ActionButton:
                    text: 'Exit'
                    size_hint_y: None
                    height: 40
                    on_press: app.stop()
    BoxLayout:
        id: table
    Label:
        id: log_label
        height: 20
        size_hint_y: None
        pos_hint: {'bottom': 1}
        text: 'Success'
        text_size: root.log_label.size
        padding_x: 10
        canvas.before:
            Color:
                rgba: .5, .5, .5, 1.0
            Rectangle:
                pos: self.pos
                size: self.size