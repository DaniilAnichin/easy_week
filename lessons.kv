<Lesson>:
    button: button
    do_rotation: False
    do_scale: False
    Label:
        text: root.__str__()
        size: 200, 200
        pos: 0, 0
        font_size: self.height / (root.lines + 3)
    Button:
        id: button
        # markup: True