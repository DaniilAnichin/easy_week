<LessonDay>:
    orientation: 'vertical'

<LessonWeek>:

<LessonSet>:
    size_hint: None, None
    width: 150
    height: len(self.children) * 100
    orientation: 'vertical'

<LessonTable>:
    pos_hint: {'top':1, 'left':1}
